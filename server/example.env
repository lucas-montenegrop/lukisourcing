DATABASE_URL=postgres://localhost:5432/lukisourcing
# WHY: Documentation is clearer when the example env shows that authentication needs a real secret instead of a placeholder value.
JWT_SECRET=replace_this_with_a_real_secret
FRONTEND_URL=http://localhost:5173
